# Development Mode Flag:
# Set VITE_USE_API=false for UI Development Mode (No database/backend required, uses mock data)
# Set VITE_USE_API=true for Database Mode (Connects to Spring Boot REST API & PostgreSQL)
VITE_USE_API=false

# Backend API URL (leave empty when using Vite proxy)
VITE_API_URL=
